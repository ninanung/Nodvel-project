<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Nodvel</title>
    <script src="https://code.jquery.com/jquery-1.11.1.js"></script>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta/css/bootstrap.min.css" integrity="sha384-/Y6pD6FV/Vv2HJnA6t+vslU6fwYXjCFtcEpHbNJ0lyAFsXTsjBbfaDjzALeQsN6M" crossorigin="anonymous">
    <style>
        * {
                margin: 0px;
                padding: 0px;
                border: 0px;
        }
        #choice {
                border: 20px;
                width: 600px;
                height: 100px;
        }
    </style>
</head>
<body>
<div style="position: relative; overflow: hidden; width: 100%; height: 100%;"> 
    <% if(pagecontents.background) { %>
        <img src="<%= pagecontents.background %>" style="position: absolute; z-index: 1; top: 0px; left: 0px; width: 1200px; height: 980px;">
    <% } else { %>
        <div style="position: absolute; z-index: 1; top: 0px; left: 0px; width: 100%; height: 100%; background-color: black;"></div>
    <% } %>
    <% if(pagecontents.character.length > 0) { %>
        <% if(pagecontents.character.length === 1) { %>
                <img src="<%= pagecontents.character[0].path %>" style="position: absolute; z-index: 2; left: 450px; top: 220px">
        <% } %>
        <% if(pagecontents.character.length === 2) { %>
                <img src="<%= pagecontents.character[0].path %>" style="position: absolute; z-index: 2; left: 300px; top: 220px">
                <img src="<%= pagecontents.character[1].path %>" style="position: absolute; z-index: 2; left: 600px; top: 220px">
        <% } %>
        <% if(pagecontents.character.length === 3) { %>
                <img src="<%= pagecontents.character[0].path %>" style="position: absolute; z-index: 2; left: 150px; top: 220px">
                <img src="<%= pagecontents.character[1].path %>" style="position: absolute; z-index: 2; left: 450px; top: 220px">
                <img src="<%= pagecontents.character[2].path %>" style="position: absolute; z-index: 2; left: 750px; top: 220px">
        <% } %>
        <% if(pagecontents.character.length === 4) { %>
                <img src="<%= pagecontents.character[0].path %>" style="position: absolute; z-index: 2; left: 450px; top: 0px">
                <img src="<%= pagecontents.character[1].path %>" style="position: absolute; z-index: 2; left: 450px; top: 300px">
                <img src="<%= pagecontents.character[2].path %>" style="position: absolute; z-index: 2; left: 450px; top: 600px">
                <img src="<%= pagecontents.character[3].path %>" style="position: absolute; z-index: 2; left: 450px; top: 900px">
        <% } %>
    <% } %>
    <% if(pagecontents.choice.length > 0) { %>
        <% if(pagecontents.choice.length === 1) { %>
            <div id="choice" style="position: absolute; z-index: 4; left: 300px; top: 390px;">
                <h3><%= pagecontents.choice[0] %></h3>
            </div>
        <% } %>
        <% if(pagecontents.choice.length === 2) { %>
            <div style="position: absolute; z-index: 4;">
                <h3><%= pagecontents.choice[0] %></h3>
            </div>
            <div style="position: absolute; z-index: 4;">
                <h3><%= pagecontents.choice[1] %></h3>
            </div>
        <% } %>
        <% if(pagecontents.choice.length === 3) { %>
            <div style="position: absolute; z-index: 4;">
                <h3><%= pagecontents.choice[0] %></h3>
            </div>
            <div style="position: absolute; z-index: 4;">
                <h3><%= pagecontents.choice[1] %></h3>
            </div>
            <div style="position: absolute; z-index: 4;">
                <h3><%= pagecontents.choice[2] %></h3>
            </div>
        <% } %>
        <% if(pagecontents.choice.length === 4) { %>
            <div style="position: absolute; z-index: 4;">
                <h3><%= pagecontents.choice[0] %></h3>
            </div>
            <div style="position: absolute; z-index: 4;">
                <h3><%= pagecontents.choice[1] %></h3>
            </div>
            <div style="position: absolute; z-index: 4;">
                <h3><%= pagecontents.choice[2] %></h3>
            </div>
            <div style="position: absolute; z-index: 4;">
                <h3><%= pagecontents.choice[3] %></h3>
            </div>
        <% } %>
    <% } %>
    <% if(pagecontents.text) { %>
        <div style="position: absolute; z-index: 3;">
            <%= pagecontents.text %>
        </div>
    <% } %>
</div>
<div class="container">
    <hr>
    <a href="/nodvel/<%= title %>/<%= prevDivergence %>/<%= prevPage %>" style="float: left;">Prev</a>
    <a href="/nodvel/<%= title %>/<%= pagecontents.nextDivergence %>/<%= pagecontents.nextPage %>" style="float: right;">Next</a>
    <hr>
    <form method="post" action="/nodvel/save/<% title %>/<% pagecontents.divergence %>/<% pagecontents.page %>">
        <select name="save" style="display: none;">
            <option value="save" selected>save</option>
        </select>
        <input type="submit" value="save" style="float: right;" class="btn btn-default">
    </form>
    <a href="/nodvel/<%= contents.title %>" style="float: left;" class="btn btn default">Main page</a>
</div>
</body>
</html>
